ip = { default = "localhost", doc = "connection URL", env = "IP" }
port = { type = "usize", default = "8080", doc = "Server port number", env = "PORT" }
enum1 = { enum = "Enum1", variants = [
  "A",
  "B",
  "C",
], default = "A", doc = "An example enum", env = "ENUM1" }
enum2 = { enum = "crate::MyEnum", doc = "An example enum", env = "ENUM2", default = "C" }
enum3 = { enum = "Enum3", variants = [
  "D",
  "E",
  "F",
],  doc = "An example enum", env = "ENUM1", optional = true }
s = {type= "crate::MySecondConfig", doc = "A nested configuration struct" , optional = true }
